#include <bits/stdc++.h>
using namespace std;

#define endl "\n"
#define int long long int

int solve(){
    int n,x;
    cin>>n>>x;

    if(x>100000) return 0;

    while(n--){
        int l;
        cin>>l;
        int a[l];
        for(int i=0;i<l;i++){
            cin>>a[l];
        }
    }
}

int32_t main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t=1;
    // cin>>t;
    while(t--){
        cout<<solve()<<endl;
    }
    return 0;
}